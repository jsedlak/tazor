@inherits DialogBase
<dialog id="@ReferenceId" closedby="any" class="flyout flyout-@(Placement.ToString().ToLower()) @GetWindowClass() @CustomClass">
    <div class="@GetWindowInnerClass()">
        <header class=@Theme.Flyout.Header>
            @if (Header != null)
            {
                @Header
            }
            else if (!string.IsNullOrWhiteSpace(Title))
            {
                <h3>@Title</h3>
            }
            @if (!HideCloseButton)
            {
                <div>
                    <a @onclick="OnCloseRequested" class="cursor-pointer opacity-75">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </a>
                </div>
            }
        </header>
        <div class=@Theme.Flyout.Content>
            @Content
        </div>
        <footer class=@Theme.Flyout.Footer>
            @Footer
        </footer>
    </div>
</dialog>