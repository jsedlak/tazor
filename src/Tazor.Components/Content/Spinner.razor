<div class="flex items-center">    
    @((MarkupString)Icon)
    <span>@Text</span>
</div>

@code {
    [Parameter]
    public MarkupString Icon { get; set; } = TazorIcons.TailwindSpinner();

    [Parameter]
    public string Text { get; set; } = "Loading...";
}
